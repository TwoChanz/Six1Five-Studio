node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment variables
.env
.env.local
.env.vercel

# Local SQLite database
local.db
local.db-shm
local.db-wal

# User uploaded files (keep directory structure with .gitkeep)
server/uploads/**/*
!server/uploads/.gitkeep

# Large asset files (deploy separately via CDN or Vercel upload)
client/public/assets/church/church-model.glb
client/public/assets/church/church-walkthrough.mp4
client/public/downloads/*.zip

# Development artifacts
repomix-output.xml
_ul

# Replit artifacts (remove from repo, keep locally ignored)
.replit
replit.nix
.local/
_transfer_reports/

# PowerShell utilities (Windows-only, not needed in deployed site)
*.ps1

# IDE and local settings
.claude/settings.local.json
.vercel
.aider*
